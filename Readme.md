# CDH Manager proxy

A proxy server for CDH Manager without need login.

## Setup

```shell
npm install
```

## Start

```shell
# check help
npm start -- -h

# start example
npm start -- -u=<username> -p=<password>
```
